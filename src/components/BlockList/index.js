import React from 'react'
import PropTypes from 'prop-types'
import StyledBlockList from './style'

function BlockList({ ...rest }) {
    return (
        <StyledBlockList {...rest} >
           
        </StyledBlockList>
    )
}

BlockList.propTypes = {

}

export default BlockList

