import React from 'react'
import PropTypes from 'prop-types'
import StyledVoiceMessage from './style'

function VoiceMessage({ ...rest }) {
    return (
        <StyledVoiceMessage {...rest} >
           
        </StyledVoiceMessage>
    )
}

VoiceMessage.propTypes = {

}

export default VoiceMessage

